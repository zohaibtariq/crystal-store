import React from 'react';

const Favourites = () => (
    <div className={`container`}>
        <h1>My Favourites</h1>
        <p>mealdb does not support any favorite api</p>
        {/* List user's favourite meals */}
    </div>
);

export default Favourites;
